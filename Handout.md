<div dir="rtl" align="right">

# Bg_Bed — מסמך מסירה
**גרסה 12 — 9 באוגוסט 2026**

מחליף את Rev 11. מסמך עצמאי: הוא לבדו אמור לאפשר לשיחה חדשה להמשיך בלי ללמוד מחדש.

---

## 1. מה זה

מערכת חכמה לחדר שינה של שמי ועירה, בנחשולים, ישראל. שני פאנלים מגע שולטים בעיסוי רטט
בשתי מיטות, ברדיו אינטרנטי, במזגן, באור, בתריס ובמאוורר. בהמשך גם תיעוד שינה
ופקודות קוליות.

**עקרון מנחה, נעול:** אפס קרינת `RF` ליד הראשים. כל התקשורת בין הלוחות קווית.
רק צומת האודיו מחזיק רדיו, והוא רחוק משני הראשים.

---

## 2. מבנה הריפו

`github.com/shemtov/Bg_Bed`, ענף `main`, תיקייה מקומית `C:\projects\Bg_Bed`

- `panel_shemi/` — הפאנל של שמי, `ESP32-8048S043`
- `panel_ira/` — הפאנל של עירה, `Waveshare ESP32-S3-Touch-LCD-4.3`
- `bedbox_shemi/` — `QuinLED-ESP32`, המיטה של שמי
- `bedbox_ira/` — `ESP32-S3-DevKitC-1`, המיטה של עירה — **טרם נכתב**
- `audionode/` — רדיו, מזגן, `RF` — ממתין ללוח
- `switcher/` — סקריפטים לגילוי ולשליטה ב-`Switcher Breeze`
- `pcb/` — נטליסט וטבלת חיבורים ללוח נושא. **הפרויקט נזנח לטובת הלחמה**
- `Handout.md` — המסמך הזה

**השמות שונו ב-9 באוגוסט.** קודם היו `panel/` ו-`bedbox/`. הסדר החדש הוא
תפקיד־ואז־שם, כדי שדומים יישבו זה ליד זה ברשימה.

**שני בד־בוקסים נפרדים בכוונה** — לוחות שונים לגמרי, `QuinLED` מול `ESP32-S3`,
מפות פינים שונות ו-`platformio.ini` שונה.

---

## 3. מוסכמות עבודה — לפי הכללים של שמי

- **תמיד קובץ שלם.** אף פעם לא `diff`, אף פעם לא "תשנה את השורה הזאת". שמי לא
  עורך קוד ביד — קלוד מבצע את השינוי ומספק את הקובץ להורדה.
- **מספר `TEST` חי בתוך הקובץ בלבד** — שלוש שורות ראשונות, שלוש אחרונות,
  ו-`#define TEST_NUMBER`. **לעולם לא בשם הקובץ.** הקובץ תמיד `src/main.cpp`.
- **תשובות בעברית**, מונחים טכניים באנגלית בתוך `code marks`.
- **`GND` נכתב באנגלית, אף פעם לא "מסה".**
- **`L298N` הוא `בקר`**, לא "נהג" ולא "דרייבר".
- `commit` דרך `GitHub Desktop`, עם מספר הגרסה בהודעה.
- כל תיקיית פרויקט נפתחת בנפרד ב-VS Code. **לעולם לא שורש הריפו.**
- נתיבים בלי אותיות עבריות — הכלים נחנקים עליהן.

---

## 4. הפאנל של שמי — `panel_shemi`

`ESP32-8048S043`, מודול `ESP32-S3-WROOM-1 N16R8`, 16MB פלאש, 8MB `PSRAM` אוקטלי,
מסך 800×480 `RGB`, מגע `GT911`.

**קושחה: `TEST 082`**

**פינים — כולם תפוסים, אין אף אחד פנוי:**

- `I2C`: `SDA` = `IO19`, `SCL` = `IO20`
- `BH1750` אור `0x23`, `SHT31` טמפ׳ ולחות `0x44`, `AT24C32` `0x57`,
  `GT911` מגע `0x5D`, `DS3231` שעון `0x68`, `BMP280` לחץ `0x76`
- `UART1`: `TX` = `IO17`, `RX` = `IO18`, דו־כיווני
- תאורה אחורית: `GPIO2`, `PWM` אמיתי
- `IO11`, `IO12`, `IO13` — **התפנו.** היו המיקרופון, שנותק

**`platformio.ini` — אסור לגעת:**
`platform = espressif32 @ 6.9.0` הכרחי בשביל `GFX 1.3.8`.
`-DLV_CONF_SKIP` — **אין `lv_conf.h` בשום מקום.** `-DLV_TICK_CUSTOM=1` הכרחי.
`-DLV_MEM_SIZE=65536`, ופונטים 20, 28, 40.

**מסך הבית — רשת 4 על 2, אריחים 175 פיקסלים:**

- שורה 0: מסאז׳, רדיו, מזגן, מאוורר
- שורה 1: אור, תריסים, הגדרות, וריק

האמנות היא 200 פיקסלים ומוקטנת ב-`lv_img_set_zoom(224)` בזמן הציור.

**מסך המסאז׳:** שתים עשרה אריחי תוכנית 4 על 3, ארבעה סליידרים אנכיים ממוספרים
1 עד 4, שורת טיימר 15/30/60 וכפתור כיבוי אדום.

**עברית בפאנל:** כל מחרוזת שמורה **הפוכה מראש**, כי `LV_CONF_SKIP` מכבה תמיכה
דו־כיוונית. **סיכון פתוח:** לא ידוע אילו גליפים נוצרו ב-`font_hebrew.h`. מילה
שמופיעה כריבועים פירושה אות חסרה, בדרך כלל סופית.

---

## 5. הפאנל של עירה — `panel_ira`

`Waveshare ESP32-S3-Touch-LCD-4.3`, אותו מודול `N16R8`.

**קושחה: `TEST 028`** — שכבת חומרה גמורה, ממשק בהתחלה.

**פינים:**

- `I2C`: `GPIO8` = `SDA`, `GPIO9` = `SCL`
- מגע `GT911` על `0x5D`, פסיקה `GPIO4`
- מרחיב `CH422G` — עונה על כל הבלוק `0x20` עד `0x3F`
- `EXIO1` = איפוס מגע, `EXIO2` = תאורה, `EXIO3` = איפוס מסך
- `GPIO6` פנוי, יוצא במחבר `Sensor AD`
- `RS-485` על `GPIO15`/`GPIO16` דרך `SP3485`

**`platformio.ini`:** אותה פלטפורמה `6.9.0`, אבל **`qio_opi`** ולא `opi_opi` —
הפלאש רגיל, ה-`PSRAM` אוקטלי. ופונטים 20, 24, 28, 40.

**מסך הבית זהה לחלוטין לשל שמי**, בכוונה: הם מחליפים צדדים.

**להוסיף:** `SHT31` על `0x44` ו-`BH1750` על **`0x5C`** — לא `0x23`, כי שם יושב
המרחיב. שניהם דרך מחבר ה-`I2C` המובנה, וה-`SHT31` על זרוע קצרה **מתחת** לקופסה.

**בלי `DS3231`** — השעה מגיעה בקו. **בלי `BMP280`** — הלחץ זהה בחדר.
**בלי מיקרופון** — הוא בצומת ההקשבה.

**התאורה האחורית היא דלוק/כבוי בלבד**, דרך המרחיב. עמעום אמיתי דורש **חוט מרגל
ה-`EN` של שבב `MP3302` ל-`GPIO6`**. **הוחלט: נדחה, לא דחוף.**

---

## 6. הבד־בוקס של שמי — `bedbox_shemi`

`QuinLED-ESP32`, שישה מנועי רטט `12V`, שלושה `בקרים` `L298N`.
**קושחה: `TEST 013`**

**מנועים:** `m0` פין 13 ראש שמאל קטן, `m1` פין 14 ראש ימין קטן,
`m2` פין 18 גב עליון **גדול**, `m3` פין 19 גב תחתון **גדול**,
`m4` פין 21 רגל שמאל קטן, `m5` פין 22 רגל ימין קטן.
אזורים `{0,0,1,2,3,3}`.

**מעבר לשמונה מנועים** — שישה גדולים ושניים קטנים, הוזמנו.
הפינים הנוספים: **`GPIO26` ו-`GPIO27`.** נבדקו, אינם `strapping`, פלט מלא.
`IO34`, `IO35`, `S_VP`, `S_VN` הם כניסה בלבד. `IO12` מסוכן — קובע מתח פלאש.

**`MIN_DUTY_BIG` 165, `MIN_DUTY_SMALL` 60**, `PWM` 20 קילו־הרץ.

**חיווט כל `בקר`:** `12V` ל-`VS`, `GND` משותף חובה, `IN1`/`IN3` ל-`GND`,
`IN2`/`IN4` ל-`5V` של אותו מודול, רק `ENA`/`ENB` ללוח.
**ג׳מפרי `ENA`/`ENB` חייבים לרדת** — אחרת המנועים על מלא וה-`PWM` לא עושה כלום.

**התקנה מוכחת:** כל מנוע על פיסת דיקט מלבנית, ארבעה ברגים ודבק חם, ואז ארבעה
ברגים לבסיס המיטה. **עבר לילה שלם.**

**`platformio.ini`: אל תצמיד את הפלטפורמה.** לא מוצמדת מגיעה לליבה 3.x,
והקוד תומך בשתיהן.

---

## 7. הבד־בוקס של עירה — `bedbox_ira`

**טרם נכתב.**

`ESP32-S3-DevKitC-1 N16R8`. **שישה מנועים, כולם גדולים**, שלושה `בקרים`.
אזורים `{0,1,1,2,2,3}` — ראש אחד, גב עליון שניים, גב תחתון שניים, רגליים אחד.

**הערה כנה:** בראש וברגליים יש מנוע יחיד, לא זוג. תוכניות שנעות לאורך הגוף
יעבדו כרגיל; תוכניות שחוצות מצד לצד ירגישו חלשות שם. עירה בחרה, וזה סביר.

---

## 8. צומת האודיו — `audionode`

**חסום. אין לוח `ESP32` פנוי.**

`TEST 004` מוכח על `ESP32-CAM` — `SomaFM` ניגן, `MP3` ב-44.1 קילו־הרץ סטריאו.
**ה-`ESP32-CAM` נפסל:** נפילות מתח עם `WiFi` ו-`I2S` יחד.

**לוח `W5500` אתרנט הגיע ועובד** — ליבה 3.x כוללת `ETH.h` עם `ETH_PHY_W5500`.

**ספרייה נעולה:** `ESP32-audioI2S @ 3.4.0`. 3.4.7 נכשל על `dsps_biquad_sf32`.
`monitor_rts` ו-`monitor_dtr` על אפס.

**חיווט `DAC` מוכח:** `VIN` 5V, `GND`, **`SCK` ל-`GND` חובה**, `BCK` `GPIO14`,
`LCK` `GPIO2`, `DIN` `GPIO13`.

**פתוח:** אינדקסי התחנות בפאנל ובצומת לא תואמים.

---

## 9. תקשורת — מעבר ל-`RS-485`

**הוחלט. חמישה מודולים אוטומטיים הוזמנו.**

`UART` פשוט עם דיודות ונגד משיכה עבד, **אבל הפאנל של עירה מוציא רק `RS-485`**
דרך `SP3485` — אין לו יציאת `UART` בלוגיקה של 3.3 וולט.

**היתרונות:** אות דיפרנציאלי חסין רעש, בנוי לריבוי מכשירים, **והדיודות מתבטלות.**
`Serial1.write()` לא משתנה.

**חיווט:** זוג מפותל, `A` ל-`A`, `B` ל-`B`, `GND` משותף.
נגד 120 אוהם **בשני קצות הקו בלבד** — בלוח של עירה יש ג׳מפר לזה.

**`CAN` נשקל ונדחה** — יתרונותיו פותרים בעיות שאין לנו, ומחייבים שכתוב פרוטוקול
בחמישה לוחות.

**פרוטוקול, ארבעה בתים `{bedId, cmd, target, value}`:**
0 כיבוי, 1 הכול, 2 אזור, 3 מנוע, 4 תוכנית, 5 טיימר, 6-9 רדיו,
10-14 מזגן, **15 `RF` — יעד 0 תריס, 1 אור, 2 מאוורר**, 30 פעימה.
עולה מהבד־בוקס: 20 רמת אזור.

---

## 10. חשמל, `EMI` וכבלים — הוחלט

**ספק 12 וולט תעשייתי אחד, 9 עד 10 אמפר, לכל המערכת.** מפיצים 12 וולט, יורדים
ל-5 **מקומית** בכל קופסה. **חיווט כוכב** בבורגי הספק.

**נתיכים, כולם איטיים:** `F1` ראשי 8 אמפר, `F2` מנועים 6 אמפר, `F3` לוגיקה
1 אמפר. הועלו אחרי המעבר לשמונה מנועים ולמיטה שנייה.
**נתיך מגן על כבל, לא על שבב.** מתג ניתוק פיזי נגיש מהמיטה הוא ההגנה האמיתית.

**קבל 1000 מיקרופאראד** על כניסת ה-12 וולט של כל `בקר`.

**הספק מבודד** — יש שנאי, אין קשר בין 230 ל-12. **ההארקה מחברת את המארז.**
מומלץ לחבר `V−` להארקה **בנקודה אחת בלבד**, בספק.

**`EMI`, לפי סדר יעילות:** קבל 0.01 מיקרופאראד על מגעי כל מנוע, מולחם **במנוע**;
פיתול זוג המנוע, סיבוב לכל 2-3 סנטימטרים; טבעת פריט על **שני** החוטים באותה
טבעת, ליד המנוע. **מארז נחושת נשקל ונדחה** — הקרינה מהחוטים, לא מהלוחות.

**כבלים:** `CAT6` אחד לכל ריצה, **זוגות שלמים** — כתום ל-12 פלוס, ירוק למינוס,
כחול `TX` ומסה, חום `RX` ומסה. **לעולם לא לפצל זוג.**
**להימנע מ-`RJ45`** — 12 וולט בשקע שנראה כמו רשת שורף מחשבים.

---

## 11. החלטות עם עירה

- **עירה רוצה מסאז׳.** סירבה בהתחלה, שינתה דעתה. **בד־בוקס שני.**
- **עירה רוצה פאנל משלה.** `Waveshare` שכבר היה בבית.
- **עירה אישרה מיקרופונים** לתיעוד נחירות, כולל בצד שלה, כולל ייחוס לצד.
  **ההסכמה היא לנחירות בלבד** — כל שימוש אחר בקול הוא שאלה חדשה.
- **ארבעה סליידרים ולא שישה** — אזור, לא מנוע.
- **תמונת פתיחה אישית לכל פאנל.** שמי על המזרן במים, עירה על הספה.
  `PANEL_ID` היא השורה היחידה שמשתנה בין הבניינים.
- **מסך הבית זהה בשני הפאנלים.** הם מחליפים צדדים.

---

## 12. מה שממתין

**חומרה בדרך:** חמישה מודולי `RS-485`, מנועים חדשים, מנתח לוגי 8 ערוצים,
קליפסי בדיקה, מודולי 433 ו-315 מגה־הרץ.

**עדיין לקנות:** `ESP32-S3-DevKitC-1 N16R8` לצומת ההקשבה, שני `INMP441`,
מודול `microSD` ב-`SPI`, מודול `A7105`, `SHT31` ו-`BH1750` לפאנל של עירה.

**לכתוב:** `bedbox_ira` מאפס, קושחת צומת האודיו, מסכי המסאז׳ והרדיו בפאנל של עירה.

**לבנות בחומרה:** נתיכים, מתג ראשי, קבלים, קבלי מנועים ופיתול, זרוע החיישן,
חלון ל-`BH1750`, וחוט ה-`MP3302` אם וכאשר.

**פתוח אצל שמי:** הכיתוב על גביש `Y1` בשלט המאוורר — 433 או 315.

---

## 13. תכנון שסוכם וטרם נבנה

**מסך מותאם למסאז׳:** ארבעה סליידרים — עוצמה, מהירות עלייה, זמן החזקה, זמן
מנוחה — על גבי לישה או פולס. שלושה חריצי שמירה, כפתור "נסה", כיוון ועומק.

**אריח טיימר:** 20, 45, 90 דקות. **התעוררות מדורגת** — מסך, אחר כך רטט קל,
אחר כך חזק יותר, ורק בסוף צליל. עצירה אוטומטית אחרי חמש דקות.
20 דקות היא שנת הצהריים הנכונה; 45 נוחתת באמצע שינה עמוקה.

**הודעות בין הפאנלים:** מילים בעברית שמורות הפוך, וסמלים כתמונות. אישור קריאה,
חותמת זמן, אריח "שוב". **עירה בוחרת את הרשימה.**

**Dreamsaver:** הקלטת חלום בהחזקת כפתור, תקרת שתי דקות, מסך כמעט חשוך,
`/dreams/` בנפרד מהיומן.

**יומן שינה:** לא אודיו — **מספרים.** אנרגיה בפס 100 עד 600 הרץ בשני ערוצים,
פעם בשנייה, כ-12 בתים. `CSV` ללילה ועוד סיכום אחד לכל הלילות.
**לתעד שלושה שבועות לפני שהמיטה זזה פעם אחת.**

**"מצב חדר שינה":** נחירה מזוהה ← הראש עולה. **פעם אחת בלילה ונשאר** — מחזור
העבודה של בקר המיטה הוא 2 דקות מתוך 20. זווית קטנה, עלייה איטית, ורק אחרי
דפוס מתמשך.

**עדכון `OTA`:** מתג בהגדרות, `WiFi` זמני עם ספירה לאחור ומסך אדום, כיבוי
אוטומטי, והפצה הלאה דרך הקו לכל השאר. **בינתיים כבל `USB` ארוך.**

---

## 14. שלטים — מה שנחקר

**מאוורר, Westinghouse, לוח `SQ19D-F20`:** גביש, סלילים ואנטנת הדפס,
**אין לד אינפרה־אדום** — כלומר רדיו. **433 או 315 עדיין לא ידוע.**
צריך **מקלט סופר־הטרודיין** — `RXB6`, `RXB12`, `SRX882`. הזול שבערכות לא מפענח.

**המיטה, בקר `CBG4-X1-3-0`:** 29 וולט, אין קשר חשמלי למערכת ה-12 וולט.
**מחזור עבודה 10% — שתי דקות דלוק, שמונה עשרה כבוי.** חייב אכיפה בתוכנה.
"יציאת `USB`" היא 13 וולט — **אין לחבר טלפון.**

**שלט `TXG-9-3`: 2.4 גיגה־הרץ, שבב `AMICCOM A7105`.** לא 433, ולכן כלי ה-`RF`
הרגילים לא רלוונטיים. הדרך: מנתח לוגי על ה-`SPI`, לקרוא מזהה, ערוץ וקצב,
ואז לשדר מ-`A7105` משלנו. **דף הנתונים ומפת הרגיסטרים פומביים.**

**שלוש חוקות בטיחות:** השלט המקורי נשאר בחדר; רק לחיצה־והחזקה מזיזה מנוע;
ומגבלת זמן קשיחה בקוד מעל מחזור העבודה.

**אור ותריסים:** 433, `FS1000A` בצומת האודיו, קידוד 1527 — אנחנו בוחרים קודים.

---

## 15. לקחים — שולם עליהם בזמן

**מהסשן האחרון, והם היקרים ביותר:**

- **`LV_TICK_CUSTOM=1` חובה.** בלי מקור זמן LVGL לא מצייר ולא בודק מגע לעולם.
  המסך קופא והמגע נראה מת בזמן שהשבב מדווח קואורדינטות מושלמות. **עשרים בניות.**
- **קונפיגורציית ה-`GT911` יכולה להכיל ערך לא חוקי.** עשר נקודות מגע כשהתקרה
  היא חמש — השבב סורק ולא מדווח כלום, ומדווח בריאות מושלמת. `ready=1, points=0`.
- **חוצץ ה-`Wire` הוא 128 בתים, והוא חותך בשקט** ומחזיר הצלחה. כתיבה של 186
  בתים "הצליחה" בלי שקרתה. **לכתוב במנות ולקרוא בחזרה.**
- **`bb_captouch` לא מתאימה ללוח הזה.** היא מזהה בקר לפי כתובות נפוצות, ואחת
  מהן `0x38` — שם יושב ה-`CH422G`. **אין דרך לכפות טיפוס.**
- **`repeated START` הכרחי לקריאת רגיסטר מ-`GT911`** — `endTransmission(false)`.
  עם `STOP` באמצע כל קריאה מחזירה אפסים.
- **`opi_opi` מול `qio_opi`:** הפלאש בלוח של עירה רגיל. `opi_opi` מפיל את הלוח
  לפני `setup()` עם `Octal Flash option selected, but EFUSE not configured`.
- **לעולם לא לצייר מסביב ל-LVGL.** ציור ישיר על מסך `RGB` שLVGL מנהלת גורם
  לשני מנועים להילחם על אותם פיקסלים — וזה ריצוד גלוי.
- **`PWM` דרך `I2C` לא יעבוד** לתאורה. כל עסקה של המגע נכנסת באמצע הפולס.
- **ידוע־טוב מנצח מסקנה.** הקוד שעובד בפאנל אחד הוא הקוד שצריך להעתיק — לא
  להסיק מדף נתונים.
- **כשתיקון עובד חלקית — יש שני מקורות ולא אחד.** כשל מלא אומר "טעית במקום";
  כשל חלקי אומר "מצאת אחד, יש עוד". קרה שלוש פעמים בפרויקט הזה.

**קודמים, עדיין תקפים:**

- **`LOW` הוא מאקרו של ארדואינו.** `const int LOW` נשבר בלי להזכיר את `LOW`.
- **ליבה 3.x הסירה `ledcSetup` ו-`ledcAttachPin`.** `ledcWrite` מקבל פין.
- **קונסולה שמגיבה רק ל-`\n` תיראה שבורה** — לקבל `CR`, `LF` או שניהם.
- **כשכלום לא עובד, שהלוח יוכיח את עצמו בלי הקונסולה.**
- **`M` בסייר של VS Code הוא סטטוס `git`, לא סטטוס שמירה.**
- **קבצים מצ׳אט נמחקים.** מה שנמסר — עולה לריפו באותו יום.
  **`bedbox TEST 0014` אבד בדיוק כך.**
- `board_build.*` ו-`board_upload.*` הם דברים שונים ושניהם נדרשים.
- `JPEGDEC` מפענח `baseline` בלבד.
- `INMP441` הוא מיקרופון 24 סיביות בחריצים של 32.
- מסך טורי יכול לעצור `ESP32` — `monitor_rts` ו-`monitor_dtr` על אפס.
- **סדר בתים בתמונות: `little endian`, בית נמוך ראשון.** הפוך נותן תמונה
  מוכרת עם צבעים משונים — הטעות הקשה ביותר לאיתור.

---

## 16. המזרן — סיכון שנסגר

הופיע ב-Rev 11 כשאלה פתוחה: מזרן לטקס וויסקו־אלסטי הוא החומר הגרוע ביותר
להעברת רעידות, והמנוע הגדול הוא 3.6 וואט מול 10 עד 24 במיטות מסחריות.

**נסגר.** שמי ישן לילה שלם על המערכת והרגיש מצוין. **מבחן הדיקט מיותר.**

---

*סוף המסמך — גרסה 12.*

</div>

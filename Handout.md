<div dir="rtl" align="right">

# Bg_Bed — מסמך מסירה
**גרסה 17 — 16 באוגוסט 2026**

מחליף את Rev 16. מסמך עצמאי: הוא לבדו אמור לאפשר לשיחה חדשה להמשיך בלי ללמוד מחדש.

## 🔁 השינוי ב-Rev 17 — הקו נעשה דו־כיווני

**הכרעה של שמי, 16 באוגוסט:** הקו לא נושא רק פקודות. **צומת האודיו מחזיר לפאנלים
את מה שמתנגן** — שם התחנה, כותרת השיר, וכל מה שהזרם מספק — **ושני הפאנלים
מציגים את זה**, של שמי ושל עירה.

עד עכשיו הפרוטוקול היה **ארבעה בתים בכיוון אחד**: הפאנל מצווה, הצומת מציית,
המסך מראה את מה שהפאנל *חושב* שקורה. מעכשיו המסך מראה את מה ש**באמת** קורה.

**זה לא סותר את מה שנכתב ב-Rev 16 על אודיו בקו.** אודיו גדול מהקו בארבעה סדרי
גודל. **טקסט קטן ממנו בשלושה.** ההבחנה נשמרת: אודיו לא עובר, טקסט עובר בקלות.

**המימוש המלא — סעיף 11.** התוספת היא מסגרת מורחבת לצד ההודעה בת ארבעת הבתים,
לא החלפה שלה. **תכנון בלבד — חומרת ה-`RS-485` טרם הגיעה ושום בית לא נשלח עדיין.**

---

## 🔒 השינוי הגדול ב-Rev 16 — כלל הפרטיות ננעל, וזיהוי עברית נפתר

### כלל נעול (שמי, 16 באוגוסט, בנחרצות)

**שום אודיו מחדר השינה לא יוצא מהחדר. לעולם.**
לא `Google Speech`, לא זיהוי בענן, לא אחסון בענן, לא העלאה מכל סוג.
**כל הזיהוי וכל ההקלטה נשארים בתוך הבית.** אין להציע שירותי ענן לפרויקט הזה שוב.

זה סוגר סופית את כיוון ה-`Google Speech` שנשקל קודם. מה שנשאר: מחשב מקומי, או
זיהוי על הצומת עצמו.

### ✅ הפתרון לעברית — `Edge Impulse` / keyword spotting

**התובנה:** זיהוי מילת מפתח הוא **סיווג צלילים, לא זיהוי דיבור.** המודל לעולם
לא לומד מה המילה אומרת ובאיזו שפה היא. הוא לומד לזהות **תבנית קול**.
**לכן עברית עובדת בדיוק כמו אנגלית** — למודל אין מושג שיש הבדל.

**כל מה שנכתב קודם על "עברית לא נתמכת" תקף לתמלול בלבד.** לטכניקה הזו זה לא חל.

- רץ על `ESP32-S3` — בדיוק צומת ההקשבה. לשבב יש מאיץ רשתות נוירונים מובנה.
- קיים פרויקט התחלה מוכן ל-`ESP32-S3-DevKitC-1` עם מיקרופון `I2S`.
- ההסקה מקומית לגמרי, שום אודיו לא עוזב בזמן שימוש.
- **אימון על הקולות של שמי ועירה עדיף על כל מנוע כללי** — אותם מיקרופונים,
  אותו מרחק, אותה אקוסטיקה. בעיה קלה בהרבה מלעבוד לכל העולם.
- **תכסיס:** לא לאמן משפטים שלמים אלא מילים קצרות ולשרשר אותן —
  wake ← מיקום ← מכשיר ← פעולה. שמונה מילים מאומנות ⇒ ~12 פעולות.

**⚠️ ההחלטה היחידה שנותרה פתוחה בכיוון הזה:** `Edge Impulse` היא פלטפורמת ענן
**לשלב האימון**. הדגימות מועלות לשרתים שלהם. זה לא אודיו פסיבי מחדר השינה אלא
הקלטה מכוונת ליד השולחן — אבל זה עדיין קול שיוצא מהבית. חלופה מקומית לחלוטין:
אימון עצמאי עם `TensorFlow Lite Micro`, יותר עבודה ובלי ממשק ווב.

**אם זה מתקבל — פקודות קול בעברית אפשריות בלי שום מחשב לינוקס.**
ההחלטה על מחשב הלינוקס מצטמצמת אז ל-`Spotify` בלבד.

---

## 🎵 Rev 15 — צומת האודיו מנגן (נשאר תקף)

**`TEST 010` רץ על חומרה, ורדיו אינטרנטי נשמע מהסאונדבר.** גלגלצ ניגן.
זו השרשרת המלאה, מאושרת מקצה לקצה:

`ESP32-S3` ← `W5500` אתרנט קווי ← ספריית אודיו ← `PCM5102A` ← ממיר אנלוגי-לדיגיטלי
← אופטי ← סאונדבר.

**התקלה שעלתה ערב שלם, והלקח:** לא נשמע כלום מ-`TEST 007` עד `TEST 009`, למרות
שהלוג הראה `playing yes` ברציפות. **הקושחה מעולם לא הייתה אשמה.** האשם היה
**קופסת הממיר האנלוגי-לדיגיטלי — היא הייתה תקולה.** החלפת הקופסה פתרה הכל.

**מה שמצא את זה — בדיקת הצליל (`TEST 009`):** בנייה שמסירה את הרשת, את הסטרימינג
ואת ספריית האודיו לגמרי, ומייצרת גל סינוס 440 הרץ בקוד ישירות אל ה-`I2S`.
**זו השיטה הנכונה לחלוקת בעיות אודיו לשניים** — היא הוציאה את הקושחה מהמשוואה
בבנייה אחת. שווה לזכור אותה לכל תקלת שמע עתידית.

---

## 1. מה זה

מערכת חכמה לחדר שינה של שמי ועירה, בנחשולים, ישראל. שני פאנלים מגע שולטים בעיסוי רטט
בשתי מיטות, ברדיו אינטרנטי, במזגן, באור, בתריס ובמאוורר. בהמשך גם תיעוד שינה
ופקודות קוליות.

**עקרון מנחה, נעול:** אפס קרינת `RF` ליד הראשים. כל התקשורת בין הלוחות קווית
(`RS-485`). רק צומת האודיו מחזיק רדיו — אך גם הוא מתחבר לרשת דרך **אתרנט קווי
(`W5500`)**, לא `WiFi`. ה-`WiFi` של כל הלוחות נשאר כבוי תמיד.

---

## 2. מבנה הריפו

`github.com/shemtov/Bg_Bed`, ענף `main`, תיקייה מקומית `C:\projects\Bg_Bed`

- `panel_shemi/` — הפאנל של שמי, `ESP32-8048S043`
- `panel_ira/` — הפאנל של עירה, `Waveshare ESP32-S3-Touch-LCD-4.3`
- `bedbox_shemi/` — `QuinLED-ESP32`, המיטה של שמי
- `bedbox_ira/` — `ESP32-S3-DevKitC-1`, המיטה של עירה — **טרם נכתב**
- `audionode/` — רדיו, מזגן, `RF` — ממתין ללוח
- `switcher/` — סקריפטים לגילוי ולשליטה ב-`Switcher Breeze`
- `pcb/` — נטליסט וטבלת חיבורים ללוח נושא. **הפרויקט נזנח לטובת הלחמה**
- `Handout.md` — המסמך הזה

**שני בד־בוקסים נפרדים בכוונה** — לוחות שונים לגמרי, `QuinLED` מול `ESP32-S3`,
מפות פינים שונות ו-`platformio.ini` שונה.

---

## 3. מוסכמות עבודה — לפי הכללים של שמי

- **תמיד קובץ שלם.** אף פעם לא `diff`, אף פעם לא "תשנה את השורה הזאת". שמי לא
  עורך קוד ביד — קלוד מבצע את השינוי ומספק את הקובץ להורדה.
- **מספר `TEST` עולה ב-1 בכל שינוי, בלי יוצא מן הכלל, בלי לקבץ.** חי בתוך הקובץ
  בלבד — באנר שלוש שורות ראשונות, `#define TEST_NUMBER`, ובאנר סוף הקובץ. שלושתם
  מתעדכנים יחד. **לעולם לא בשם הקובץ.** הקובץ תמיד `src/main.cpp`.
- **תשובות בעברית**, מונחים טכניים באנגלית בתוך `code marks`.
- **`GND` נכתב באנגלית, אף פעם לא "מסה".**
- **`L298N` הוא `בקר`**, לא "נהג" ולא "דרייבר".
- **לעולם לא לטעון שמשהו עובד/תוקן אלא אם נבדק על חומרה אמיתית.** שפה זהירה:
  "לא נבדק, יש לבנות לאישור".
- **לעולם לא לנחש.** לעבוד רק מעובדות — פלט טורי, צילומי מסך, תמונות. כשתקועים,
  לעצור ולאסוף עובדה (הדפסה טורית, סיבת reset) במקום להמשיך לנחש.
- **קישורי קניה:** תמיד `AliExpress` ראשון, `Temu` שני, `Amazon` שלישי. קישור
  ישיר לדף המוצר.
- `commit` דרך `GitHub Desktop`, עם מספר הגרסה בהודעה.
- כל תיקיית פרויקט נפתחת בנפרד ב-VS Code. **לעולם לא שורש הריפו.**
- נתיבים בלי אותיות עבריות — הכלים נחנקים עליהן.
- **מלכודת שכפול הקובץ:** הדבקה ל-VS Code לפעמים **מוסיפה** לקוד הישן במקום להחליף.
  התיקון: לחיצה ימנית על `main.cpp` ← `Delete` ← לגרור את הקובץ החדש ל-`src/`.

---

## 4. הפאנל של שמי — `panel_shemi`

`ESP32-8048S043`, מודול `ESP32-S3-WROOM-1 N16R8`, 16MB פלאש, 8MB `PSRAM` אוקטלי,
מסך 800×480 `RGB`, מגע `GT911`.

**קושחה: `TEST 085` — מותקן ועובד בחדר השינה.**

**פינים — כולם תפוסים, אין אף אחד פנוי:**

- `I2C`: `SDA` = `IO19`, `SCL` = `IO20`
- `BH1750` אור `0x23`, `SHT31` טמפ׳ ולחות `0x44`, `AT24C32` `0x57`,
  `GT911` מגע `0x5D`, `DS3231` שעון `0x68`, `BMP280` לחץ `0x76`
- `UART1`: `TX` = `IO17`, `RX` = `IO18`, דו־כיווני
- תאורה אחורית: `GPIO2`, `PWM` אמיתי
- `IO11`, `IO12`, `IO13` — **התפנו.** היו המיקרופון, שנותק

**`platformio.ini` — אסור לגעת:**
`platform = espressif32 @ 6.9.0` הכרחי בשביל `GFX 1.3.8`.
`-DLV_CONF_SKIP` — **אין `lv_conf.h` בשום מקום.** `-DLV_TICK_CUSTOM=1` הכרחי.

**מסך הבית — רשת 4 על 2, אריחים 175 פיקסלים:**
שורה 0: מסאז׳, רדיו, מזגן, מאוורר. שורה 1: אור, תריסים, הגדרות, וריק.

**ה-screensaver של שמי** הוא מכונת מצבים נקייה: `ST_UI` / `ST_SAVER` / `ST_PREVIEW`.
במצב saver LVGL מושהה, המגע נקרא ישירות, והשעון מצויר ב-`gfx` גולמי (raw). זה
המודל שהוכיח את עצמו — וממנו נלמד ה-screensaver החדש של עירה (סעיף 5).

---

## 5. הפאנל של עירה — `panel_ira`

`Waveshare ESP32-S3-Touch-LCD-4.3`, אותו מודול `N16R8`.

**קושחה: `TEST 079` — כל המסכים עובדים ומאושרים על החומרה.**

**מה בנוי ועובד (מאושר על החומרה):**
- מסך בית עם רשת אריחים (מסאז׳, רדיו, מזגן, מאוורר, אור, תריסים, הגדרות)
- מסך רדיו (`buildRadio`), מסך מסאז׳ (`buildMassage`), מסך הגדרות (`buildSettings`)
- מסך מזגן (`buildAC`) ומסך מאוורר (`buildFan`) — מועתקים מ-`panel_shemi`
- **כותרת עליונה במסך הבית** (`TEST 077`): שמאל `BG חדר שינה` (עברית), מרכז
  `TEST NNN`, ימין טמפרטורה + תאריך/שעה (placeholder עד שהשעון יגיע בקו)

**ה-screensaver — נכתב מחדש מהיסוד ב-`TEST 072`, ועבר דרך ייסורים ארוכים:**
- **המודל החדש:** ה-screensaver **אינו** מסך LVGL. הוא מצויר ישירות ב-`gfx`
  גולמי מעל מסך שחור, ודגל פשוט `saverActive` עוקב אחריו. זה חיסל **שתי** בעיות
  שעלו ימים: החפיפה של המסך הקודם, וההיתקעות במגע.
- **לקח מרכזי (`TEST 074`):** כשה-saver פעיל, **אסור לקרוא ל-`lv_timer_handler`**.
  אחרת מסך `About` (המתעדכן על טיימר) מצייר את עצמו **מעל** ה-saver. זו הסיבה
  שרק `About` חפף — הוא המסך היחיד שמתרענן תקופתית.
- **יציאה:** המגע נקרא ישירות בלולאה. שליש ימני = דרים־קאטצ'ר (החזקה = הקלטת
  חלום, stub טורי עד שצומת ההקשבה ייבנה). כל מגע אחר = יציאה הביתה.
- **מראה (`TEST 075`+):** חוגה אמיתית (`img_dial` 340×340) ודרים־קאטצ'ר אמיתי
  (`img_dream`) מצוירים ב-`draw16bitRGBBitmap`. מחוגים כקווים ב-10:10. הטמפרטורה
  בפונט `gfx` מובנה — **הערה:** הפונט המעוגל `FreeSansBold24pt7b` **לא קיים**
  בהתקנת ה-`GFX` הזו; אם רוצים פונט מעוגל צריך להמיר את `temp_big` לפורמט GFXfont.

**משימות פתוחות במסך הבית (לטסט הבא):** התאריך צריך לשבת **משמאל** לטמפרטורה,
לא מתחתיה.

**פינים:**
- `I2C`: `GPIO8` = `SDA`, `GPIO9` = `SCL`
- מגע `GT911` על `0x5D`, פסיקה `GPIO4`
- מרחיב `CH422G` — עונה על כל הבלוק `0x20` עד `0x3F`.
  `EXIO1` = איפוס מגע, `EXIO2` = תאורה, `EXIO3` = איפוס מסך
- `GPIO6` פנוי, יוצא במחבר `Sensor AD`
- `RS-485` על `GPIO15`/`GPIO16` דרך `SP3485`

**`platformio.ini`:** אותה פלטפורמה `6.9.0`, אבל **`qio_opi`** ולא `opi_opi` —
הפלאש רגיל, ה-`PSRAM` אוקטלי. `board_build.*` **ו-**`board_upload.*` שניהם נדרשים.
פונטים 20, 24, 28, 40. `monitor_rts`/`monitor_dtr` על אפס.

**חיישנים:** `SHT31` על `0x44` ו-`BH1750` על **`0x5C`** — לא `0x23`, כי שם יושב
המרחיב. **בלי `DS3231`** — השעה מגיעה בקו. **בלי `BMP280`**. **בלי מיקרופון** —
הוא בצומת ההקשבה/האודיו.

**קונפליקט מספרי פקודה שתוקן:** `CMD_AC_LIGHT` הועבר ל-`17` (היה `16`, התנגש
עם `CMD_AC_TIMER`).

---

## 6. הבד־בוקס של שמי — `bedbox_shemi`

`QuinLED-ESP32`, שישה מנועי רטט `12V`, שלושה `בקרים` `L298N`.
**קושחה: `TEST 012`**

**מנועים:** `m0` פין 13, `m1` פין 14, `m2` פין 18 (גדול), `m3` פין 19 (גדול),
`m4` פין 21, `m5` פין 22. אזורים `{0,0,1,2,3,3}`.
פינים נוספים למעבר לשמונה מנועים: **`GPIO26` ו-`GPIO27`** (נבדקו, לא `strapping`).

**חיווט כל `בקר`:** `12V` ל-`VS`, `GND` משותף חובה, `IN1`/`IN3` ל-`GND`,
`IN2`/`IN4` ל-`5V`, רק `ENA`/`ENB` ללוח. **ג׳מפרי `ENA`/`ENB` חייבים לרדת.**
`PWM` 20 קילו־הרץ. **התקנה עברה לילה שלם.**

**מנועים אושרו:** `12V`; גדולים `0.3A`, קטנים `0.1A`; שניהם מסתובבים גם ב-`5V`.

---

## 7. הבד־בוקס של עירה — `bedbox_ira`

**טרם נכתב.** `ESP32-S3-DevKitC-1 N16R8`. שמונה מנועים, ארבעה `בקרים`.
**`QuinLED` עבור `bedbox_ira` עדיין לא הגיע.**

---

## 8. צומת האודיו — `audionode`

**החומרה הגיעה, והלוח נבנה והולחם.** (id 9 בקו)

### ✅ מאושר על חומרה — `TEST 010` מנגן רדיו

**מצב נוכחי: עובד.** מנגן גלגלצ מהסאונדבר, מתחיל לבד בלי מחשב מחובר.

**פקודות בטורי:** `l` רשימת תחנות · מספר לניגון · `+`/`-` עוצמה · `s` עצירה ·
`i` מידע · **`u <url>` לנגן כל כתובת שהיא — בלי בנייה מחדש.** זו הדרך לצוד תחנות
חדשות בלי לגעת בקוד.

**`autoplay`** מוגדר לתחנה 0, שלוש שניות אחרי קבלת כתובת. לביטול: `AUTOPLAY_STATION` ל-`-1`.

**תחנות בטבלה (כתובות מאומתות מרשימות ציבוריות):**
| # | תחנה | כתובת |
|---|---|---|
| 0 | גלגלצ | `http://glzwizzlv.bynetcdn.com/glglz_mp3` — `http` פשוט, הידידותי ביותר |
| 1 | כאן 88 | `https://playerservices.streamtheworld.com/api/livestream-redirect/KAN_88.mp3` |
| 2 | Groove Salad | `https://ice5.somafm.com/groovesalad-128-mp3` |
| 3 | Drone Zone | `https://ice5.somafm.com/dronezone-128-mp3` |
| 4 | Deep Space One | `https://ice5.somafm.com/deepspaceone-128-mp3` |
| 5 | Lush | `https://ice5.somafm.com/lush-128-mp3` |

`SomaFM` מזהירים ש-`ice5` יכול לזוז; שרת חלופי הוא `ice3` באותו נתיב.

**שמי ביקש וטרם נמצאו:** whiskey blues, מפלי מים/טבע, מדיטציה, `Pink Floyd Radio`.
לצוד אותן עם פקודת `u`.

**ספריית האודיו:** `https://github.com/schreibfaul1/ESP32-audioI2S.git` — **git master,
לא הנעילה הישנה ל-`3.4.0`.** הנעילה ההיא נבחרה לליבה 2.x על ה-`ESP32-CAM` ואינה
מתאימה כאן. master עובד עם ליבה 3.x.

### מה מאושר על חומרה — `TEST 006`, בדיקת האתרנט

הקושחה נכתבה **מאפס** (לא העתקה מ-`TEST 004`) ורצה על הלוח החדש. פלט הטורי אישר:

- `chip` — `ESP32-S3` rev 2, שני ליבות, 240 מגה־הרץ
- `flash` — `16777216` בתים. **16 מגה־בייט מאושר.**
- `psram` — `8388608` בתים. **8 מגה־בייט מאושר ועובד.** (תיאור הלוח ב-`PlatformIO`
  טוען "No PSRAM" — **התיאור שגוי**, השבב עצמו מדווח אחרת. לא ללכת לפי התיאור.)
- `reset` — `power on`. אתחול נקי, בלי נפילת מתח.
- `eth` — `LINK UP` תוך שתי שניות, כתובת תוך שלוש
- כתובת שהתקבלה: `192.168.1.195`, `gateway`/`dns` `192.168.1.1`
- `MAC` — `12:C1:9F:CD:37:C9` ← **לשריין כתובת קבועה בנתב**
- מהירות — `100 Mbps full duplex`
- פתירת שם ופתיחת `socket` החוצה — **הצליחו**
- יציבות — 84 שניות רצופות, `heap` `311744` **זהה בכל שורה**, אפס דליפה

### מיפוי הפינים — מאושר על חומרה, לא הצעה

| רכיב | פין | `GPIO` |
|---|---|---|
| `W5500` | `SCLK` | 12 |
| `W5500` | `MOSI` | 11 |
| `W5500` | `MISO` | 10 |
| `W5500` | `SCS` | 9 |
| `W5500` | `INT` | 8 |
| `W5500` | `RST` | 7 |
| `PCM5102A` | `BCK` | 14 |
| `PCM5102A` | `LCK` | 2 |
| `PCM5102A` | `DIN` | 13 |

**אסור לגעת:** `GPIO26` עד `GPIO37` — תפוסים על ידי הפלאש והזיכרון החיצוני.
פינים שנשארו פנויים: 1, 4, 5, 6, 15, 21, 38, 47, 48.
שמור לקו `RS-485`: `TX` 17, `RX` 18, בקרת כיוון 16 (רק אם הממיר דורש).

### חומרה — מה שיושב על הלוח

- מוח: `ESP32-S3-WROOM-1 N16R8`
- אתרנט: `W5500` (HanRun HR911105A). **מאושר: למודול יש מייצב על הלוח והזנה
  בחמישה וולט תקינה.** הכניסות סובלות חמישה וולט, אין צורך בממיר מתחים.
- יציאת אודיו: `PCM5102A` → `3.5mm` → ממיר אנלוגי-לדיגיטלי → אופטי → סאונדבר.
  **אין צורך במגבר `TPA3116`.**

### גשרי ההלחמה של ה-`PCM5102A` — מאומת מהתיעוד

| גשר | תפקיד | לאן |
|---|---|---|
| `H1L` | `FLT` מסנן | `L` |
| `H2L` | `DEMP` הדגשה | `L` |
| `H3L` | `XSMT` השתקה | **`H`** |
| `H4L` | `FMT` פורמט | `L` |
| רפידה ליד פין השעון | `SCK` | ל-`GND` |

**`H3L` לצד הנמוך = השתקה מוחלטת, אפס צליל.** זו הסיבה הנפוצה ביותר ל"אין קול",
וברבים מהלוחות זה מצב ברירת המחדל מהמפעל.

### סביבת הפיתוח — מה שנפתר בכאב

`audionode` הוא הפרויקט היחיד שרץ על ליבה 3.x. הפאנלים נשארים על
`espressif32 @ 6.9.0`. **לא לאחד ביניהם.**

```ini
platform = https://github.com/pioarduino/platform-espressif32/releases/download/stable/platform-espressif32.zip
board = esp32-s3-devkitc-1
board_build.arduino.memory_type = qio_opi
board_build.partitions = default_16MB.csv
board_upload.flash_size = 16MB
board_upload.maximum_size = 16777216
```

שלוש תקלות התקנה שנפתרו, לפי הסדר:
1. `Failed to install Python dependencies into penv` — הסביבה הווירטואלית פגומה.
   פתרון: לשנות שם ל-`penv_old` ב-`C:\Users\User\.platformio`, לתת לה להיבנות מחדש.
2. `ModuleNotFoundError: No module named 'certifi'` — אותה בעיה, ביטוי אחר.
3. `FileNotFoundError` על נתיב ארוך במיוחד — **מגבלת 260 התווים של `Windows`.**
   פתרון: `regedit` → `HKEY_LOCAL_MACHINE\SYSTEM\CurrentControlSet\Control\FileSystem`
   → `LongPathsEnabled` = `1` → **אתחול מחשב** → למחוק `.platformio\.cache`.

**שגיאות אדומות של `Access is denied` על קבצי `.git` בתוך חבילת ה-framework —
לא מזיקות.** זו פעולת ניקוי שנכשלת, לא שלב בנייה.

### מלכודות `API` בליבה 3.x

`ETH` **אין** לו `hostByName`. במקום זה: `NetworkClient::connect(hostname, port, timeout)`
פותר שם ופותח `socket` בקריאה אחת, ואז `client.remoteIP()` מחזיר את הכתובת.

### `TEST 011` → `TEST 014` — נכתבו, טרם רצו על חומרה

| `TEST` | מה נוסף |
|---|---|
| 011 | דף רשת על פורט 80 — מה מתנגן, עוצמה, עצירה, כפתורי תחנות, תיבת כתובת חופשית, ו-6 `presets` **שמורים בפלאש** (שורדים ניתוק חשמל) |
| 012 | מתג `USE_WIFI` (ברירת מחדל 0 = קווי). מצב שולחן עבודה נבדק ועבד מול `Zyxel_C46D` |
| 013 | `OTA` דרך הרשת · דף `/log` · **טרמינל רשת חי על פורט 2323** |
| 014 | תיקון שורה אחת — הצהרה מקדימה של `handleLine` מעל `pollLogServer` |

**`OTA`:** שם מארח `audionode`, סיסמה `bgbed`. הרדיו **נעצר אוטומטית** לפני צריבה
כדי ששידור לא יתנגש בהעלאה. ב-`platformio.ini` יש שלוש שורות מוכנות בהערה
(`upload_protocol = espota`).

**טרמינל הרשת (פורט 2323):** מתחברים מה-`PC` בכל תוכנת טרמינל. מקבלים מיד את
ההיסטוריה, ואז פלט חי — **ואפשר להקליד את אותן פקודות** של המוניטור בכבל.
זה מה שמחליף את כבל ה-`USB` באמת.

**דף `/log`:** 120 השורות האחרונות, קריא מהטלפון. כפתור בתחתית דף הרדיו.

> **⚠️ להשאיר את כבל ה-`USB` נגיש** עד ש-`OTA` יעבוד כמה פעמים. העלאה שנכשלת
> באמצע עשויה לדרוש כבל כדי להתאושש. **לא לסגור את הלוח מאחורי רהיט באותו יום
> שמפעילים `OTA` בפעם הראשונה.**

> **⚠️ סיכון פרטיות בריפו:** ב-`TEST 012` סיסמת ה-`WiFi` נכתבה לתוך הקובץ,
> **והריפו ציבורי.** לפני `commit` — להחזיר `placeholder` ולוודא `USE_WIFI 0`.

### פתוח

- אינדקסי התחנות בפאנל ובצומת לא תואמים — **הפתרון נבחר ב-Rev 17:** הצומת
  שולח את השמות בקו (מטען `0x02`), והפאנל מפסיק לנחש. ראה סעיף 11
- **לבנות ולצרוב `TEST 014` בכבל** (אי אפשר להתקין `OTA` דרך `OTA`)
- דף הרשת מעולם לא רץ על חומרה
- **החלטת איחוד `audionode` + `listener`** — ראה סעיף 10

---

## 9. צומת ההקשבה — `listener` (id 8)

**החומרה ביד** — הלוח והמיקרופונים. **הקושחה טרם נכתבה.**

תפקיד: להאזין לשינה של שני האנשים ליד המיטה — **נחירות, נשימה, ותיעוד שינה**,
וכן **פקודות קול** ותיעוד חלומות (Dreamsaver). הכל קווי, אפס `RF`.

**חומרה:**
- מוח: `ESP32-S3-DevKitC-1 N16R8` (ל-`DevKitC` אין חריץ כרטיס משלו)
- **שני `INMP441`** — מיקרופון אחד ליד כל ראש (זרוע קצרה, כבל מסוכך)
- אחסון — **הוכרע 16 באוגוסט: לקנות מודול `microSD` פשוט ב-`SPI`.**

  **תיקון לגרסה הקודמת:** נכתב קודם ש-`SD_MMC` בסיב אחד הוא "הנכון אם מקליטים
  אודיו". **החישוב מראה שזה היה זהיר מדי.** ההקלטה היא 16 קילו־הרץ, 16 סיביות,
  מונו — כ-**32 קילו־בייט לשנייה**. `SPI` מעביר את זה במרווח ביטחון עצום.
  **`SPI` רגיל מספיק לגמרי**, פחות פינים וקוד פשוט יותר.

  **⚠️ שני מודולים שונים לגמרי נקראים "3.3 וולט":**
  - **המודול הכחול הנפוץ** נושא מייצב `AMS1117` וממיר רמות `74LVC125`. הוא בנוי
    ל-`Arduino` של חמישה וולט. **בהזנת 3.3 וולט הוא לרוב לא מצליח לעלות** — למייצב
    אין מספיק מרווח. זו התלונה הנפוצה ביותר על הלוחות האלה, ו"הפתרון" של אנשים
    (להזין חמישה וולט) שם לוגיקה של חמישה וולט על קווי הכרטיס.
  - **מה שצריך:** הסוג החשוף — רק השקע וכמה רכיבים פסיביים, **בלי מייצב ובלי ממיר
    רמות**. לוח קטן יותר, שישה פינים, מסומן 3.3 וולט בלבד.

  **לקנות כמה** — עולים אגורות, לאחד תהיה הלחמה קרה, וכדאי אחד על השולחן ואחד במיטה.
  **כרטיס קטן** — 8 או 16 ג'יגה־בייט, `Class 10`. כרטיסים ענקיים לא מהירים יותר
  ולפעמים בררניים יותר בעלייה.

- **❌ נדחה: `MP3-TF-16P` / `DFPlayer Mini`.** שמי שאל אם אפשר להשתמש בו כקורא
  כרטיסים. **לא — וזו סיבה מבנית, לא פרט שאפשר לעקוף.** זה נגן `MP3` שלם:
  שקע, מפענח, `DAC` ומגבר קטן. **חריץ הכרטיס מחובר לשבב המפענח שבתוך המודול,
  לא למעבד.** ה-`ESP32` לעולם לא רואה את הכרטיס ולא יכול לפתוח בו קובץ.
  הוא יכול רק לנגן קבצים שכבר הועלו מ-`PC` — **כלומר נכשל בדיוק במה שנדרש:
  כתיבה.** בנוסף, לוחות `V3.0` הם שכפולים עם כמה שבבי מפענח שונים בפנים שלא
  מגיבים זהה לאותן פקודות.

**התכסיס שחוסך פינים — שני מיקרופונים על אותם שלושה חוטים:**
ל-`INMP441` יש פין `L/R`. **קושרים `#1` ל-`GND`** (ערוץ שמאל = כרית שמי)
**ו-`#2` ל-`3V3`** (ערוץ ימין = כרית עירה). שניהם חולקים `BCK`, `WS`, `SD` —
**אפס פינים נוספים**, שני ערוצי `I2S` בפריים אחד. ההשוואה בין הערוצים היא מה
שמייחס נחירה לצד הנכון (הפרש ≥6 `dB`, אחרת "לא ודאי").

**חיבור לקו:** `RX` מהקו התחתון, `TX` לקו העליון דרך דיודה `1N4148`, 12V אחרי
`F3`, `GND` לנקודת הכוכב.

**הוחלט 15 באוגוסט — אין מיקרופון באף פאנל.** המיקרופונים חיים אך ורק על צומת
ההקשבה. בפאנל של שמי זה כבר המצב בפועל (`IO11`/`IO12`/`IO13` התפנו). בפאנל של
עירה מעולם לא היה. **תוצאה:** חריצי הכרטיס בשני הפאנלים משתחררים לשימוש.
בפאנל של עירה הכרטיס עובד ב-`SPI` ופין ה-`SD_CS` מונע דרך `EXIO4` של ה-`CH422G`,
כלומר **אפס פינים נוספים של המעבד**. בפאנל של שמי צריך עוד לוודא את מצב הפין
הרביעי (`CS`).

**רעיון שנשקל ונדחה:** לוח שלישי ייעודי להקלטת חלומות. נדחה כי המיקרופונים כבר
על צומת ההקשבה, התוספת לקושחה זעירה, והלוח השלישי לא פותר את ההשמעה אלא מזיז
אותה. **הפתרון המועדף:** ההקלטה יושבת על צומת ההקשבה, ולהשמעה נמתח **חוט ייעודי
קצר** בין צומת ההקשבה לצומת האודיו — לא הקו הראשי. בקצב גבוה זה מעביר ~90 קילו־בייט
לשנייה מול ~8 שנדרשים. פי עשר ממה שצריך.

---

## 9א. מחקר הנחירות ודום הנשימה — מה שנמצא (לא לבזבז שוב)

**זיהוי אקוסטי של נחירות ודום נשימה עובד — מגובה במחקר:**
- מחקר `JCSM`: טלפון על החזה של 50 מטופלים בזמן `polysomnography`. זמן נחירה
  התאם למעבדה `r=0.93`; מדד ה-`RDI` המשוער התאם ל-`AHI` האמיתי `r=0.94`.
  לאבחון `OSA` ב-`AHI≥15`: רגישות 0.70, סגוליות 0.94.
- גישת `ML` על צלילי נשימה: **רגישות 94%, סגוליות 87%** על 14,500+ קטעים של
  30 שניות; ה-`AHI` המשוער נבדל בממוצע ב-4.3 אירועים לשעה.
- אלגוריתם מסחרי לזיהוי נחירה: רגישות 86.3%, סגוליות 99.5% — **נחירה עצמה קלה
  לזיהוי אמין.**

**המנגנון — איך מזהים דום נשימה (חשוב!):** לא מזהים דום נשימה על ידי שקט. שקט
דו־משמעי (נשימה שקטה, מיטה ריקה, ראש מופנה — כולם נשמעים זהה). מזהים אותו לפי
**התבנית: נחירה סדירה ← שתיקה ארוכה חריגה ← נשיפת התאוששות (gasp) חזקה.**
ה-`gasp` הוא מה שהופך את זה מזיהוי לניחוש.

**האלגוריתם (רוב החלקים כבר נבנו ל-wake word):**
1. דגימה ב-16 קילו־הרץ, 24 סיביות בחריצים של 32, סטריאו
2. **אנרגיית פס:** נחירה חיה בעיקר מתחת ל-600 הרץ. `bandpass` 100–600 הרץ +
   `RMS` לכל פריים של 25 מילי־שניות — **בלי `MFCC`**
3. רצפת רעש אדפטיבית (כבר קיים מ-`TEST 056`)
4. **אירוע נחירה** = פרץ 0.3–2.5 שניות, מעל הרצפה, **חוזר כל 2–5 שניות** —
   המחזוריות היא קצב הנשימה, וזה מה שמבדיל נחירה ממכונית חולפת
5. **ייחוס** לפי הפרש ערוצים
6. **מועמד לדום נשימה** = היה נוחר סדיר, אז **≥10 שניות שקט**, שמסתיים ב**פרץ
   חזק מהממוצע האחרון**. שלושת התנאים — אחרת מתעד כל יציאה לשירותים
7. **לתעד, לא לפעול** — בהתחלה

**מה לעשות עם זיהוי — ההמלצה החזקה:** **שלושה שבועות רק להקליט, בלי לפעול.**
כל בוקר הפאנל מציג שורה אחת לכל אדם: דקות נחירה, נחירות לשעה, מספר הפסקות מעל
10 שניות, ההפסקה הארוכה. **הלוג שווה יותר מכל רטט** — זו דאטה לרופא, וזו הדרך
היחידה לדעת אם התערבות עושה משהו. רטט מליל ראשון = לעולם לא תדע את קו הבסיס.

**גבולות כנים:** דום נשימה **שקט** בלתי נראה (מרכזי, או חסימתי בלי נחירה — אין
חתימה אקוסטית). **אין מדידת חמצן** — ה-`desaturation` הוא מה שמזיק, ומיקרופון
לא רואה אותו. גם המחקר עם הטלפון על החזה החמיץ ~30% מהמקרים הבינוניים. **זה כלי
סינון ותיעוד, לא אבחון.** אם הלוג מראה הפסקות סדירות — זה הרגע להזמין בדיקת שינה
(זמינה בקופות בישראל), לא לכוונן ספים.

**פרטיות — הוחלט:** **אודיו לעולם לא נשמר — רק אנרגיות פס וחותמות זמן.** שום
דבר לא עוזב את הרכיב (אין `RF`). עירה אישרה מיקרופונים **לנחירות בלבד**.

**בקרת מיקום המיטה (התגובה) — מה שנמצא:**
- **הדרך המוכחת היא מניעתית, לא תגובתית:** לזהות שכיבה על הגב ולעודד גלגול —
  לא לזהות דום נשימה ולעצור אותו. פטנט `US 11,096,849` (Sleep Number):
  מצב נגד־נחירות שמרים מעט את הראש (הרמה קטנה מפחיתה רטט רקמה רכה, וגם מעודדת
  שינוי תנוחה). זה בדיוק "מצב חדר שינה" שלנו.
- **אזהרה הנדסית:** מחקרים על רטט לעצירת דום נשימה השתמשו ב-200–300 הרץ (הפס
  של קולטני `Pacinian`). **מנועי ה-`ERM` שלנו רחוקים מזה** — ב-12V הם 50–66
  הרץ. יורגש כרעד, לא כזמזום החד של המחקרים. לכן **התגובה שלנו היא הרמת ראש,
  לא רטט לעצירת אירוע.**
- זירת הפטנטים צפופה מאוד (משפחת Franceschetti, ZEEQ smart pillow ועוד). **לא
  מעכב בנייה לעצמנו — רלוונטי רק אם אי־פעם נמכור.**

---

## 10. זיהוי קול ופקודות — סטטוס ההחלטות

### ⚠️ תיקון חשוב 15 באוגוסט — `Vosk` **אינו** תומך בעברית

רשימת השפות הנתמכות של `Vosk` כוללת יותר מעשרים שפות — אנגלית, גרמנית, ערבית,
פרסית, יוונית, טורקית ועוד — **ועברית אינה ברשימה.** אין מודל עברית. הכיוון הזה
סגור, ומה שכתוב בגרסאות קודמות של המסמך היה שגוי.

**`Whisper` כן תומך בעברית**, ורץ על `Raspberry Pi 5`: שני המודלים הקטנים ביותר
עומדים בקצב דיבור חי, והבינוני רץ בערך בחצי מהמהירות ומתאים רק לעיבוד ברקע.

**אבל הדיוק בעברית נמוך.** מודל עברית קטן מכוונן נעצר סביב **55% שגיאת מילים** —
יותר ממחצית המילים שגויות.

**המסקנה המעשית:** המספר הזה תקף ל**תמלול חופשי**. לרשימה **סגורה** של עשר פקודות
ידועות מראש הוא הרבה פחות רלוונטי — משווים את התוצאה לעשר האפשרויות ובוחרים את
הקרובה. **פקודות עברית קבועות: אפשרי. הכתבה חופשית בעברית: לא.**
(שווה לבדוק את `ivrit-ai`, פרויקט ישראלי עם נתוני אימון בעברית.)

**הוחלט מוקדם בפרויקט, ועדיין תקף:**
- **`ESP-SR` (של Espressif) לא תומך בעברית**, ורץ רק על `ESP32-S3`.
- זיהוי קול חופשי (כמו Alexa) דורש שרת ענן — מתנגש עם עקרון הפרטיות/`RF`.

**מה שכן אפשרי:** wake word (למשל "BG") + מספר **פקודות אנגלית קבועות מראש**
(`ESP-SR` MultiNet, עד ~200 פקודות, מקומית על `ESP32-S3`).

**שמי מעוניין בפקודות קול** ("BG start radio", "BG start Spotify", "BG start
massage") כדי לא ללחוץ על מסכים כל הזמן. **שיקול עומס פתוח:** אם ה-`audionode`
עושה בו-זמנית אתרנט + רדיו + `DAC` + הקלטה + `ESP-SR`, זה אולי יותר מדי ל-`S3`
אחד. אפשרות: מוח `S3` נפרד לזיהוי קול.

### 16 באוגוסט — שתי הכרעות ששינו את התמונה

**1. הענן סגור.** ראה הכלל הנעול בראש המסמך. `Google Speech` ירד מהשולחן.

**2. `Edge Impulse` פותר את העברית מקומית.** ראה בראש המסמך. אם שלב האימון
בענן מתקבל — **אין צורך במחשב לינוקס בשביל קול בכלל.**

### ❌ למה אי אפשר להעביר אודיו ב-`RS-485`

שמי שאל אם צומת ההקשבה יכול לתפוס פקודה ולשלוח אותה בקו לניתוח. **הרעיון נכון,
הצינור שגוי:**

שלוש שניות דיבור ≈ **100,000 בייט**. הקו רץ ב-115200 סיביות לשנייה ≈ **11,000
בייט לשנייה** במקרה הטוב, והוא משותף עם העיסוי, השעון והשאר.
כלומר שלוש שניות אודיו יתפסו את **כל הקו לעשר שניות** ויחסמו כל מכשיר אחר.
הקו תוכנן להודעות של ארבעה בייטים. אודיו גדול ממנו בארבעה סדרי גודל.

**שלושה נתיבים שכן עובדים:**
1. אתרנט משלו לצומת ההקשבה (`W5500` נוסף) — הקו נשאר פנוי.
2. **דרך צומת האודיו** — החוט הפרטי בין הצמתים כבר מתוכנן להשמעת חלומות.
   לשלוח את האודיו באותו חוט, וצומת האודיו (שכבר ברשת) מעביר הלאה.
   **אפס חומרה חדשה — זה הנתיב המועדף.**
3. מיקרופון ישירות על המזהה — פשוט בתוכנה, אבל כבל מיקרופון ארוך ליד חיווט
   מנועים הוא בדיוק המקום שבו רעש חשמלי מנצח.

**הארכיטקטורה הנכונה:** מילת ההשכמה מזוהה **על הצומת עצמו** — מקומי וזול, וזה
אומר שהמיקרופונים **לא משדרים כל הלילה**, רק שלוש שניות אחרי מילת ההשכמה.
אחר כך השלוש שניות עולות בחוט הפרטי, ו**פקודה של ארבעה בייטים חוזרת בקו.**
קטן נכנס, קטן יוצא. הקו נושא רק את התוצאה.

**עירה אישרה מיקרופונים** לתיעוד נחירות בלבד — כל שימוש אחר בקול הוא שאלה חדשה.

---

### ⚠️ ההחלטה הפתוחה הגדולה ביותר בפרויקט — האם נכנס מחשב לינוקס קטן

**ההבחנה שמפשטת הכל:** `Spotify` דורש לינוקס. עברית דורשת לינוקס. **אלו לא שתי
בעיות אלא אותה בעיה.** מי שפותר אחת מקבל את השנייה כמעט בחינם.

**אם נכנס מחשב:** `Spotify` דרך `librespot` (בוגר ויציב, שנות אור מהפתרון המשובץ),
עברית דרך `Whisper`, ואפשר גם להעביר אליו את הרדיו.

**הבעיה שנשארת פתוחה גם אז:** המיקרופונים חייבים להיות ליד הראשים, המחשב חייב
להיות רחוק מהמיטה. שלוש דרכים להעביר קול ביניהם:
1. מילת הפעלה מזוהה מקומית על הלוח שליד המיטה, ורק ~3 שניות נשלחות למחשב
   (~90 אלף בתים, שנייה אחת על חוט ייעודי מהיר). זול, אבל מילת ההפעלה באנגלית בלבד.
2. חיבור רשת משלו ללוח שליד המיטה — נקי, אבל עוד מודול ועוד חוט.
3. מיקרופון ישירות למחשב בכבל ארוך — פשוט תוכנתית, מסוכן מבחינת רעש חשמלי.

**התקשורת עם המחשב היא לא הבעיה** — שלוש דרכים, כולן עובדות: להצטרף לקו הקיים
עם ממיר (הוזמנו חמישה), לדבר דרך הרשת (שניהם קוויים וכבר על אותה רשת), או חוט
טורי ישיר. **המכשול היחיד בקו:** לינוקס לא מדייק בתזמון שחרור הקו כמו מיקרובקר —
לבחור ממיר שמחליף כיוון **בחומרה**, לא כזה שדורש שליטה בתוכנה.

**הסיכונים האמיתיים במחשב הם במקום אחר:** כרטיס זיכרון שנשחק בעבודה רצופה
(לאתחל ממכונן חיצוני או מערכת קבצים לקריאה בלבד); זמן עלייה של חצי דקה מול
שנייה במיקרובקר — **כל מה שקריטי נשאר על הלוחות**; מאוורר בחדר שינה = כישלון,
צריך קירור פסיבי או מיקום מחוץ לחדר; ורשת אלחוטית ובלוטות׳ מובנים שצריך לכבות
בהגדרה.

**ההחלטה טרם התקבלה.**

---

### `Spotify` — מה שנבדק לעומק 15 באוגוסט

**הפתרון המשובץ (`cspot`) — לא מומלץ:**
- לא ספרייה שמוסיפים לרשימת התלויות. נבנה ב-`ESP-IDF` עם `CMake`, `protobuf`
  ו-`mbedTLS`. מתנגש עם ספריית האודיו שלנו על אותה יציאה.
- פורמט מוצפן וכבד לפענוח מ-`MP3`. דיווחי קריסות ואיפוסים ברגע ההתחברות.
- מונדס לאחור — נשבר כש-`Spotify` משנה משהו.
- **הרוצח:** `cspot` הוא **מקלט** בלבד, ומכשירי `cspot` **לא מופיעים** ברשימת
  המכשירים של ממשק הרשת של `Spotify`. אין למי לשלוח פקודה. **הפאנל לא יוכל לשלוט.**

**הפתרון על לינוקס (`librespot`) — עובד, ומאומת אצל אחרים:**
- קיים משתמש שמריץ בדיוק את התרחיש הזה: מחשב קטן שנשלט מטאבלט על הקיר ומפקודות קול.
- **התנאי הקריטי:** להריץ אותו **עם פרטי ההזדהות** של החשבון. זה מה שהופך אותו
  לנשלט מבחוץ. אם הוא רק "מתגלה ברשת" — הוא לא בהכרח נשלט.
- **מלכודת שפוגעת בדיוק בתרחיש שלנו:** העברת ניגון עובדת רק אם `Spotify` ניגן
  ברבע השעה האחרונה. התקן שישב בטל כל הלילה — בעייתי. יש גם דיווחים שההתקן נעלם
  מהרשימה אחרי שעות של חוסר פעילות.
- **הפתרון למלכודת:** לא לשלוח את פקודת הניגון דרך `Spotify` בכלל. המחשב מריץ
  שירות קטן משלו שמאזין לפאנל **ישירות**, והוא אומר לנגן המקומי מה לעשות.
  ממשק הרשת של `Spotify` נחוץ אז רק למשוך שמות פלייליסטים.
- **משפטית:** הפרויקט עצמו כותב שהשימוש בקוד הזה מול `Spotify` כנראה אסור מבחינתם,
  והשימוש על אחריות המשתמש.

**איך זה ייראה על המסך:** הפאנל **לא יקבל מסך מהמחשב** — אין בו דפדפן ואין שולחן
עבודה מרוחק. במקום זה: המחשב מושך את שמות הפלייליסטים ושולח **טקסט בלבד** (כמה
מאות בתים), **הפאנל מצייר את הרשימה בעצמו** באותו סגנון של שאר המסכים, נגיעה
בשורה שולחת מספר, והמחשב מנגן. מבחינת המשתמש זה בדיוק מה שביקש.
**מגבלות:** רשימה קצרה (~20 שורות), לא חיפוש בכל הקטלוג; ושמות בעברית דורשים
כיסוי מלא בפונט של הפאנל.

**המלצה:** לא לשים את `Spotify` בבניית צומת האודיו. לסיים אתרנט ורדיו, ולהשאיר
את `Spotify` כשאלה נפרדת שתלויה בהחלטת המחשב.

---

## 11. תקשורת — `RS-485`

**הוחלט.** הפאנל של עירה מוציא **רק `RS-485`** דרך `SP3485`. חמישה מודולי `RS-485`
הוזמנו. **ה-transceiver עבור `panel_shemi` הוזמן — טרם הגיע (דגם מדויק נדרש).**

**חיווט:** זוג מפותל, `A` ל-`A`, `B` ל-`B`, `GND` משותף. נגד 120 אוהם **בשני
קצות הקו בלבד**. `panel_shemi` הוא ה-bus arbiter; השעון (`DS3231`) עובר ממנו
לכל האחרים בקו.

**פרוטוקול, ארבעה בתים `{bedId, cmd, target, value}`:**
- `0` כיבוי, `1` הכול, `2` אזור, `3` מנוע, `4` תוכנית, `5` טיימר
- `10` `AC_POWER`, `11` `AC_TEMP`, `12` `AC_MODE`, `13` `AC_FAN`, `14` `AC_SWING`,
  `16` `AC_TIMER`, `17` `AC_LIGHT`
- `15` `RF` — **יעד 0 תריס, 1 אור, 2 מאוורר**
- `18` `FAN_POWER`, `19` `FAN_SPEED`, `20` `FAN_TIMER`, `21` `FAN_LIGHT`
- `22` `RADIO_PLAY` (יעד = אינדקס תחנה), `23` `RADIO_STOP`, `24` `RADIO_VOL`
  (0..21), `25` `RADIO_SLEEP` (דקות)
- עולה מהבד־בוקס: `20` רמת אזור

> **התנגשות מספרים שקיימת כבר היום:** `20` משמש גם `FAN_TIMER` בירידה וגם "רמת
> אזור" בעלייה. זה עובד רק כי הכיוון מפריד ביניהם. **קודים חדשים לא ממחזרים
> מספר קיים** — ראה 26/27 למטה.

---

### 🔁 Rev 17 — הערוץ החוזר: מה מתנגן עכשיו

**מה שהתבקש:** שם התחנה, כותרת מה שמתנגן, וכל מה שהזרם מוסר — חוזר בקו ומוצג
**בשני הפאנלים**.

#### למה זה אפשרי, בניגוד לאודיו

שלוש שניות דיבור הן כמאה אלף בתים — ולכן אודיו לא עובר בקו (סעיף 10).
**כותרת שיר היא 60 עד 120 בתים, ומשתנה פעם בשלוש דקות.** גם אם שואלים כל שתי
שניות ומקבלים 120 בתים בכל פעם, זה כשישים בתים לשנייה — **בערך חצי אחוז מהקו.**
אודיו גדול מהקו פי עשרת אלפים. טקסט קטן ממנו פי אלף. אלו לא אותה שאלה.

#### הבעיה: לארבעה בתים אין כותרת

ההודעה הקיימת היא ארבעה בתים קבועים בלי סימן התחלה. אי אפשר פשוט "להאריך" אותה —
המקבל לא ידע איפה היא נגמרת. **הפתרון: בית פתיחה `0xAA`**, שלעולם אינו `bedId`
חוקי (המזהים הם 1, 2, 8, 9). המקבל קורא בית אחד ויודע מיד באיזה סוג הודעה מדובר:
`0xAA` ⇒ מסגרת מורחבת, כל דבר אחר ⇒ ארבעה בתים כרגיל. **הפרוטוקול הישן לא משתנה
באף בית.**

#### המסגרת המורחבת

| שדה | גודל | תוכן |
|---|---|---|
| `START` | 1 | `0xAA` |
| `DST` | 1 | יעד; `0` = לכולם |
| `SRC` | 1 | שולח (`9` = צומת האודיו) |
| `TYPE` | 1 | סוג המטען |
| `LEN` | 1 | אורך המטען, 0 עד 200 |
| `PAYLOAD` | `LEN` | טקסט `UTF-8` |
| `CRC` | 1 | `CRC-8` על `DST` עד סוף המטען |

תקורה: שישה בתים. מסגרת מלאה במקרה הגרוע — 206 בתים, **כ-18 מילי־שניות על הקו.**

**סוגי מטען:**

| `TYPE` | שם | תוכן |
|---|---|---|
| `0x01` | `NOW_PLAYING` | שם התחנה, בית אפס, כותרת השיר |
| `0x02` | `STATION` | בית אינדקס + שם התחנה — **סוגר את אי-ההתאמה בין הפאנל לצומת** |
| `0x03` | `STATUS` | מנגן/עצור, עוצמה, קצב סיביות |

#### מי מדבר ומתי — הכלל של הקו נשמר

`panel_shemi` הוא ה-bus arbiter. **צומת האודיו לא פותח פה מיוזמתו, אף פעם.**
הוא צובר את המידע ומחכה שישאלו.

- **שאלה:** ארבעה בתים `{9, 26, 0, gen}` = `RADIO_POLL`. `value` הוא מספר הדור
  האחרון שהפאנל ראה.
- **תשובה כשאין חדש:** ארבעה בתים `{9, 27, 0, gen}` = `RADIO_NOCHANGE`.
  **ארבעה בתים, לא מאתיים** — וזה המצב ברוב הפעמים.
- **תשובה כשיש חדש:** מסגרת מורחבת מסוג `0x01`.

**מתי שואלים:** רק כשהרדיו מנגן או כשמסך הרדיו פתוח, כל שתי שניות.
בשאר הזמן הקו שקט לגמרי.

#### עירה מקבלת את זה בלי בקשה

הקו הוא multi-drop — **כולם שומעים הכול.** `panel_ira` פשוט קורא מסגרות עם
`DST` אפס ומצייר אותן. **אפס תעבורה נוספת ואפס לוגיקת שכפול בין הפאנלים.**
זה בדיוק אותו יתרון שכבר קיים בהעברת השעון.

#### מאיפה הטקסט מגיע בצומת

ספריית האודיו כבר מוסרת את זה בקריאות חוזרות: `audio_showstation()` שם התחנה,
`audio_showstreamtitle()` כותרת `ICY` (בדרך כלל "אמן - שיר"), `audio_bitrate()`,
`audio_info()`. הצומת שומר את האחרון, מקדם את מונה הדור, **ומחכה.**

#### ⚠️ מה שחייב אימות על חומרה — הקידוד

**מטא-דאטה של `ICY` אינה בהכרח `UTF-8`.** תחנות שונות שולחות `UTF-8`,
`Windows-1255` או `Latin-1`, ואין דרך לדעת מראש מי שולחת מה. **לא לנחש.**
הצעד הראשון בבנייה: להדפיס את הבתים הגולמיים ב-hex לטרמינל לכל תחנה, ורק אז
להחליט — המרה לפי תחנה, או זיהוי אוטומטי.

**כיסוי הפונט:** כותרת בעברית תלויה בכיסוי של `font_hebrew.h`. תווים לטיניים
עם סימני ניקוד וגרשיים מסולסלים נופלים מחוץ לכיסוי — **להחליף תו לא מוכר ברווח**,
לא להשאיר ריבוע ולא להתרסק.

**אורך:** הקיצוץ ל-200 בתים נעשה **בצומת**, לא בפאנל. בפאנל כותרת ארוכה גוללת
(`LV_LABEL_LONG_SCROLL_CIRC`), והגלילה נעצרת כשה-screensaver פעיל — מאותה סיבה
שבגללה `lv_timer_handler` לא נקרא שם.

#### סטטוס

**תכנון בלבד.** חומרת ה-`RS-485` טרם הגיעה, המסגרת המורחבת מעולם לא נשלחה,
וכלום מזה לא רץ. אין שורת קוד אחת.

---

## 12. חשמל, `EMI` וכבלים — הוחלט

**ספק 12 וולט תעשייתי אחד, 9 עד 10 אמפר, לכל המערכת.** מפיצים 12 וולט, יורדים
ל-5 **מקומית** בכל קופסה. **חיווט כוכב** בבורגי הספק.

**נתיכים איטיים:** `F1` ראשי 8A, `F2` מנועים 6A, `F3` לוגיקה 1A.
**קבל 1000 מיקרופאראד** על כניסת ה-12 וולט של כל `בקר`. **מתג ניתוק פיזי** נגיש
מהמיטה הוא ההגנה האמיתית.

**`EMI`, לפי סדר יעילות:** קבל `0.01µF` על מגעי כל מנוע, מולחם **במנוע**; פיתול
זוג המנוע; טבעת פריט על **שני** החוטים. **מארז נחושת נדחה** — הקרינה מהחוטים.

**כבלים:** `CAT6`, **זוגות שלמים** — כתום ל-12+, ירוק ל-12−, כחול `TX`+מסה, חום
`RX`+מסה. **לעולם לא לפצל זוג. להימנע מ-`RJ45`** — 12V בשקע רשת שורף מחשבים.

---

## 13. החלטות עם עירה

- **עירה רוצה מסאז׳** ← בד־בוקס שני. **פאנל משלה** ← `Waveshare`.
- **אישרה מיקרופונים לנחירות בלבד**, כולל ייחוס לצד.
- **ארבעה סליידרים ולא שישה** — אזור, לא מנוע.
- **תמונת פתיחה אישית לכל פאנל.** `PANEL_ID` היא השורה היחידה שמשתנה.
- **מסך הבית זהה בשני הפאנלים** — הם מחליפים צדדים. פרט אחד שונה: לפאנל של עירה
  **אין** פקדי כוונון שעה/תאריך (מקבלת שעה מ-`panel_shemi` בקו).

---

## 14. מה שממתין

**חומרה בדרך:** מודולי `RS-485`, מנועים חדשים, מנתח לוגי, מודולי 433/315 מגה־הרץ.
`transceiver RS-485` ל-`panel_shemi`. `QuinLED` ל-`bedbox_ira`.

**סטטוס קושחה נוכחי:** `panel_shemi` `TEST 085` · `panel_ira` `TEST 079` ·
`bedbox_shemi` `TEST 012` · **`audionode` `TEST 014` — נמסר, טרם נבנה. `TEST 010`
הוא האחרון שאושר על חומרה ומנגן רדיו** · `bedbox_ira` טרם נכתב · `listener` טרם נכתב.

**החומרה של צומת האודיו ביד:** `ESP32-S3-WROOM-1 N16R8`, `W5500`, `PCM5102A`,
`INMP441` — ראה סעיף 8.

**עדיין לקנות:**
- **מודול `microSD` 3.3 וולט בלבד, בלי מייצב ובלי ממיר רמות** (לצומת ההקשבה) —
  ראה סעיף 9 להסבר על שני הסוגים. לקנות כמה. כרטיס 8–16 ג'יגה־בייט `Class 10`.
- מודול `A7105` (לשלט המיטה)
- כבל `Cat6` נחושת מלאה

**לכתוב:** `listener` מאפס (**הצעד הבא — העלאת המיקרופונים**), `bedbox_ira` מאפס,
פרוטוקול הקו בצד צומת האודיו **כולל המסגרת המורחבת של Rev 17** (סעיף 11),
מסך רדיו בפאנלים **עם שורת "מה מתנגן" שמוזנת מהקו, בשני הפאנלים**.

**הצעדים המיידיים לפי סדר:**
1. לבנות ולצרוב `TEST 014` **בכבל**. לוודא שלוש שורות חדשות אחרי קבלת כתובת:
   `OTA` מוכן, פורט הלוג, ורשימת התחנות.
2. לבדוק `OTA`, את `/log` ואת פורט 2323.
3. **להחזיר `placeholder` לסיסמת ה-`WiFi` ולוודא `USE_WIFI 0`**, ואז `commit`
   ו-`push` של `TEST 010`→`014` ושל המסמך הזה.
4. לצוד את ארבע התחנות החסרות (ויסקי בלוז, מפלים/טבע, מדיטציה, פינק פלויד)
   ולשמור כ-`presets`.
5. למשוך כבל `Cat6` חדש לחדר השינה. **נחושת מלאה, לא נחושת מצופה אלומיניום.**
   למשוך שני מקטעים בזמן שהחוט בקיר.
6. לשריין את `12:C1:9F:CD:37:C9` בנתב.
7. לאמת את מספרי הפינים של ה-`RS-485` ב-`panel_ira` מול הסכמה של `Waveshare`.

**`OTA` — מוצדק עכשיו.** הצומת עובד, ולכן כבל ה-`USB` הפך למטרד ולא לחבל הצלה.
טבלת המחיצות `default_16MB.csv` כבר תומכת בזה (שתי מחיצות אפליקציה). להוסיף
שם מארח וסיסמה כדי שאף אחד אחר ברשת לא יוכל לצרוב.

**הוראה מפורשת של שמי (14 באוגוסט):** קושחת צומת האודיו נכתבת **מאפס**, לא
מועתקת מ-`TEST 004`. הבנייה הראשונה על ה-`ESP32-CAM` עלתה לילה שלם והוא לא רוצה
לחזור על זה.

**הוראה מפורשת נוספת:** **ארכיטקטורה לפני בנייה.** ארבע המשימות — רדיו, `Spotify`,
צומת ההקשבה, וזיהוי עברית — צריכות להיות מובנות והקשר ביניהן סגור **לפני**
מיפוי פינים וקוד. (עודכן: האתרנט נבנה בכל זאת, כי הוא היה נדרש בכל תרחיש
ארכיטקטוני, והיה הסיכון הגדול ביותר.)

**לבנות בחומרה:** נתיכים, מתג ראשי, קבלים, זרוע החיישן, חלון ל-`BH1750`.

---

## 15. תכנון שסוכם וטרם נבנה

### `TEST 011` — דף רשת בצומת האודיו (השלב הבא, לא תלוי בשום חומרה חדשה)

שמי שאל אם אפשר לחפש מוזיקה מהטלפון, להשאיר את הטלפון מחוץ לחדר, ושהמוזיקה
תתנגן בחדר השינה. **התשובה כן, והיא מתאימה בדיוק לעקרונות הפרויקט.**

פותחים בדפדפן הטלפון את `192.168.1.195`, מקבלים דף עם תיבת חיפוש, מקישים על
תוצאה — והחדר מתחיל לנגן. **הטלפון לא מזרים את האודיו**, הוא רק שולח פקודה קצרה.
המוזיקה מגיעה מהאינטרנט ישירות לצומת הקווי. אפשר להניח את הטלפון וללכת.

זה שונה מהותית מ-`Bluetooth` או `AirPlay`, שבהם הטלפון הוא המקור וחייב להישאר
בטווח — ומחזירים `RF` לכיוון המיטה. כאן לא.

**מה שצריך לבדוק:** הטלפון על אלחוטי והצומת על כבל. שניהם על אותו נתב, אז הם
אמורים לראות זה את זה. יש נתבים שמבודדים לקוחות אלחוטיים מקוויים — אם זה דלוק,
זו הגדרה לכבות, לא תכנון מחדש.

**מה שהדף לא יכסה:** פלייליסטים אישיים ב-`Spotify`. זה עדיין דורש את מחשב
הלינוקס. רדיו ו-`Spotify` נשארות שתי בעיות נפרדות.

**ולמה זה לא עוקף:** אותו דף הופך להיות הדרך לנהל presets לתמיד, והפאנל אחר כך
רק מציג את מה ששמרת בו ושולח מספר בקו. הפאנל לעולם לא מחפש.

**דפדפן על הפאנל — לא אפשרי.** הפאנל הוא מיקרובקר עם מאות קילו־בייטים של זיכרון
עבודה. דפדפן מודרני דורש מנוע `HTML`, מנוע `JavaScript`, `TLS` ומחולל פונטים.
בנוסף הפאנל בכלל לא מחובר לרשת — רק צומת האודיו קווי.

---

**תלוי ב-`RS-485` + `listener` + `audionode` (אף אחד לא נבנה):**

- **Dreamsaver:** שני אריחים חדשים בשני הפאנלים — הקלטת חלום (החזקה) והשמעה
  (אחרי 07:00). ה-UI שולח פקודה בקו; ההקלטה על ה-`listener`; ההשמעה דרך
  ה-`audionode`. **ה-UI של המקליט כבר קיים ב-screensaver כ-stub טורי.**
- **יומן שינה:** לא אודיו — **מספרים** (אנרגיה בפס 100–600 הרץ). לתעד שלושה
  שבועות לפני שהמיטה זזה.
- **"מצב חדר שינה":** נחירה מזוהה ← הראש עולה. פעם אחת בלילה, זווית קטנה.
  בקר המיטה `CBG4-X1-3-0`: **מחזור עבודה 10%** — חייב אכיפה בתוכנה.
  **המחקר המלא על נחירות, דום נשימה, ובקרת המיקום — בסעיף 9א.**
- **התעוררות מדורגת:** מסך, רטט קל, חזק יותר, ורק בסוף צליל.
- **Spotify:** רק דרך `cspot`/`euphonium` (Spotify Connect, `Premium`, ניסיוני).

**שלטים שנחקרו:**
- מאוורר Westinghouse `SQ19D-F20`: רדיו (433 או 315 — טרם ידוע). צריך מקלט
  סופר־הטרודין.
- שלט מיטה `TXG-9-3`: **2.4 גיגה־הרץ, שבב `AMICCOM A7105`** — לא 433.
- אור ותריסים: 433, `FS1000A` בצומת האודיו, קידוד 1527.

---

## 16. לקחים — שולם עליהם בזמן

**מלכודת החלפת הקבצים — קרתה שלוש פעמים (16 באוגוסט, פעם רביעית).**
`main.cpp` ו-`platformio.ini` נגררים זה למקומו של זה, והתוצאה היא עשרות שגיאות
הידור חסרות פשר ("`TEST` does not name a type" על שורת באנר).
**לבדוק את השורה הראשונה לפני כל בנייה:**
`main.cpp` מתחיל ב-`/* ===` · `platformio.ini` מתחיל ב-`; ===`.
**מה שיעצור את זה:** לנקות עותקים ישנים מתיקיית ההורדות לפני כל זוג קבצים חדש,
או להוריד קובץ אחד בכל פעם ולאשר אותו לפני הבא.

**להצהיר פונקציה לפני שמשתמשים בה (16 באוגוסט).** `C++` קורא מלמעלה למטה.
`pollLogServer` קרא ל-`handleLine` שהוגדר מתחתיו — שגיאה אמיתית שלי.
הצהרה מקדימה אחת פתרה. **בקוד ארוך בקובץ אחד, זה יחזור.**

**"סיווג" ו"זיהוי" הם לא אותו דבר (16 באוגוסט).** ההנחה שעברית חסומה הייתה
נכונה לתמלול ושגויה לזיהוי מילות מפתח. **לפני שסוגרים כיוון בגלל שפה — לשאול
אם המודל בכלל צריך לדעת מה השפה.**

**בדיקת חשבון לפני שבוחרים פתרון (16 באוגוסט).** ההמלצה ל-`SD_MMC` הייתה זהירה
מדי; חישוב פשוט (32 קילו־בייט לשנייה) הראה ש-`SPI` מספיק בגדול. **מספר אחד
חסך פינים, קוד וזמן.**

**בדיקת צליל לפני שמאשימים את הקוד (15 באוגוסט, עלה ערב שלם):**
כשהלוג מראה שהכל מנגן ואין קול — לבנות בנייה שמסירה את הרשת ואת ספריית האודיו
ומייצרת צליל בקוד ישירות אל ה-`I2S`. אם הצליל נשמע — החומרה תקינה והבעיה בתוכנה.
אם לא — התוכנה יצאה מהמשוואה. **חצי שעה של בנייה חוסכת ערב של ניחושים.**
במקרה שלנו: הצליל לא נשמע, ואז החלפת קופסת הממיר פתרה הכל.

**חשד לרכיב פסיבי כשכל השאר מוכח.** "מבחן האצבע" בקופסת הממיר נתן זמזום, ולחיצת
`reset` נתנה קליק בסאונדבר — שניהם נראו כאילו הם מוכיחים שהקופסה עובדת. **הם לא.**
הם הוכיחו רק שהחיווט מוליך. קופסה יכולה להעביר טרנזיאנט ולא לדגום אודיו רציף.

**הקוד לא מנגן לבד אם לא אמרת לו.** `TEST 007` חיכה לפקודה בטורי. ברגע שהלוח עבר
להזנת המיטה בלי `USB`, הוא עלה, קיבל כתובת, וישב בשקט לנצח. **מכשיר בחדר שינה
חייב לנגן בלי מחשב מחובר** — מאז `TEST 008` יש `autoplay` אוטומטי.

**קבצים שמתחלפים בהורדה.** `main.cpp` ו-`platformio.ini` הוחלפו זה בזה בגרירה,
והתוצאה הייתה 41 שגיאות הידור חסרות פשר. **לבדוק את השורה הראשונה לפני בנייה:**
`main.cpp` מתחיל ב-`/* ===`, ו-`platformio.ini` מתחיל ב-`; ===`.


**מהסשן האחרון (ה-screensaver של עירה) — היקרים ביותר:**

- **ה-screensaver לא צריך להיות מסך LVGL.** ציור `gfx` גולמי מעל מסך שחור +
  דגל `bool` פשוט = אין חפיפה ואין היתקעות. מסך LVGL עם כפתור מסך-מלא = ימים
  אבודים.
- **כשה-saver הגולמי פעיל, אסור לקרוא ל-`lv_timer_handler`.** אחרת מסך שמתרענן
  על טיימר (`About`) מצייר את עצמו מעל ה-saver.
- **`bootReport()` כל 20 שניות נראה כמו reboot אבל אינו.** אין `ESP-ROM`, ה-heap
  יציב = המערכת חיה. לא לפרש הדפסה חוזרת כאתחול.
- **שני קוראי מגע מתחרים על אותו `GT911` = בעיה.** קורא אחד "גונב" את המגע מהשני
  (מנקה את הדגל). קורא אחד בלבד בכל רגע.
- **פונט `FreeSansBold24pt7b` לא קיים בהתקנת ה-`GFX` הזו.** `#include "Fonts/..."`
  נכשל. הפונט המובנה תמיד זמין.

**קודמים, עדיין תקפים:**
- **`LV_TICK_CUSTOM=1` חובה.** בלעדיו LVGL לא מצייר לעולם (~20 בניות אבודות).
- **קונפיגורציית `GT911`: מקסימום 5 נקודות מגע** (לא 10). ערך לא חוקי גורם
  `ready=1, points=0` לנצח.
- **חוצץ ה-`Wire` הוא 128 בתים וחותך בשקט.** לכתוב במנות.
- **`bb_captouch` לא מתאימה** — מבלבלת בין `CH422G` ל-FocalTech.
- **`repeated START` הכרחי לקריאת `GT911`** — `endTransmission(false)`.
- **`qio_opi` (לא `opi_opi`)** — פלאש quad, `PSRAM` אוקטלי.
- **לעולם לא לצייר ישירות מעל LVGL על מסך `RGB`** — שני מנועים נלחמים = ריצוד.
- **ידוע־טוב מנצח מסקנה.** להעתיק קוד עובד, לא להסיק מדף נתונים.
- **סדר בתים בתמונות: `little endian`.** `draw16bitRGBBitmap` ל-LITTLE,
  `draw16bitBeRGBBitmap` ל-BIG.
- **ליבה 3.x הסירה `ledcSetup`/`ledcAttachPin`.**
- **קבצים מצ׳אט נמחקים.** מה שנמסר עולה לריפו באותו יום.
- **`i2cLock` עם `portMAX_DELAY`** — לא לתחום בזמן; timeout שבר את ניקוי דגל
  ה-`GT911`.

---

## 17. המזרן — סיכון שנסגר

**נסגר.** שמי ישן לילה שלם על המערכת והרגיש מצוין.

---

*סוף המסמך — גרסה 17.*

</div>
